import React from "react";
// @ts-ignore
import {connect} from "react-redux";
interface Props {

}
const CreateTodoPageContainer = (props :Props) => {
    return (<>Create Page</>)
}

export default connect()(CreateTodoPageContainer);
